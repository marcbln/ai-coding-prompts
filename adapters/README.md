# Adapters

This directory contains configurations that connect the content of this library
to various AI coding tools.

## `symlink/`

A symlink-based adapter for exporting skills and workflows to specific AI agent
config directories. See [`agent_exports.yaml`](symlink/agent_exports.yaml) for
the configured agent targets.
